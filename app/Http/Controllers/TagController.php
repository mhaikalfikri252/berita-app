<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class TagController extends Controller
{
    public function index(Request $request, $id) {
        
        // DB::table('post_tag')
        //     ->where('post_id', $id);

        // return redirect("/tag/{{asset('')}}"}}); 
    }
}
